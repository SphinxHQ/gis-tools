# 13:45 - 14:00 | 修复 CI 中 pnpm-lock.yaml 与 package.json 不同步问题

### 🧭 用户在想什么
用户关注 CI 流水线的稳定性，发现 `pnpm install` 在 frozen-lockfile 模式下失败，需要快速定位并修复 lockfile 不一致问题以恢复 CI 运行。

### 📦 用户提了什么
- "git 的 ci Run pnpm install ERR_PNPM_OUTDATED_LOCKFILE Cannot install with "frozen-lockfile" because pnpm-lock.yaml is not up to date with <ROOT>/package.json"
- 附带完整的错误日志，包含 lockfile 中 specifiers 与 package.json 中 specs 的对比

### 💭 为什么这样想
上一个提交 `4eabada feat: TopoJSON支持+结构树改进...` 新增了 topojson-client、topojson-server 及其 @types 依赖到 package.json，但未同步更新 pnpm-lock.yaml。CI 环境默认启用 frozen-lockfile，严格校验 lockfile 与 package.json 一致性，因此触发失败。用户希望保持 CI 的严格校验机制（而非用 `--no-frozen-lockfile` 绕过），从根源修复 lockfile。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| pnpm-lock.yaml | 修改 | 本地 `pnpm install` 重新生成，新增 38 行补全 topojson 相关依赖记录 |

提交记录：
- commit: `542d977` chore(deps): 更新 pnpm-lock.yaml 同步 topojson 依赖
- 推送至 origin/main（4eabada..542d977）

### 🔮 下一步去哪里
1. 观察 CI 是否顺利通过，确认 frozen-lockfile 校验恢复正常
2. 可考虑在 CI 流水线中加入 lockfile 一致性预检或 husky pre-commit 钩子，避免类似遗漏再次发生
