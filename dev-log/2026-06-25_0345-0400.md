# 03:45 - 04:00 | 历史记录移动端双行布局与坐标系按钮溢出修复

### 🧭 用户在想什么
用户持续关注移动端历史记录 tab 的排版细节，从"换行、内容丢失"问题逐步深入到"双行分界不明显"、"第二排信息布局"、"数字过大"等精细化调整。同时发现绘制图形 tab 的坐标系按钮存在文字溢出，质疑 AI 为何未发现该问题。用户思维聚焦于：信息密度与可读性的平衡、移动端小屏幕下的数据展示策略、检验流程的完备性。

### 📦 用户提了什么
- "历史记录里面的数据不完整。是老的数据。你把类型信息补上我要看效果"
- "历史那里 两排没有明显分界"
- "第二排左侧是类型图标， 右侧是 左右胶囊数字， 左是要素个数，右是顶点个数"
- "这个数字要小。超过1000 用k"
- "我要告诉你绘制图形- 坐标系按钮， 文字溢出了。你为什么没发现。怎么才能发现。也记录起来"

### 💭 为什么这样想
历史记录是数据管理的核心入口，移动端屏幕有限，传统6列表格无法完整展示。用户通过逐步反馈引导 AI 从"隐藏次要列"→"双行合并列"→"第二排图标+胶囊数字"的演进，本质是在有限空间内最大化信息密度同时保持可读性。坐标系按钮溢出问题的提出，是因为 AI 之前只检查了移动端（span隐藏前缀），未检查桌面端（完整显示"EPSG:4490"）的 scrollWidth vs clientWidth，反映出检验流程在断点覆盖上的盲区。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| `src/components/data/GisDataReader.vue` | 修改 | 历史记录移动端改为2列布局（时间+名称/类型合并列），第二排：类型图标+胶囊数字（要素数蓝/顶点数绿），添加 formatCount 函数（≥1000显示k），移除刷新/清空按钮，输入文本 tab 改为简单 textarea |
| `src/components/gismap/MapControlPanel.vue` | 修改 | 坐标系按钮添加 flex-shrink:0、min-width:auto、padding:0 8px，修复桌面端文字溢出 |
| `index.html` | 修改 | 开屏 loading canvas 位置从 38.2% 屏高提升到 28% 屏高 |
| `dev-log/temp/phase1-inspection-rules-v2.md` | 补充 | 新增 L5-12 按钮文字溢出检验（scrollWidth对比）、L5-13 flex容器内按钮压缩检验 |

### 🔮 下一步去哪里
用户可能继续验证其他 tab 在不同分辨率下的表现，或要求将坐标系按钮溢出的检验方式固化为自动化检查流程。历史记录的胶囊数字样式可能需要进一步微调（颜色、大小、间距）。
