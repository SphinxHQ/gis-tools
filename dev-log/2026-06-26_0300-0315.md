# 03:00 - 03:15 | 修复转换预览污染原数据集地图缓存（5.4）

### 🧭 用户在想什么
用户在验证 013 任务修复后，实际操作"读取 4490 → 转 4524 → 切回 4490"时发现地图 projection 未切回 4490，经纬度坐标显示在顶上一条线。用户进一步强调"数据集之前完全独立，完全隔离，各自的地图也是"，表达了对数据集隔离性的核心诉求。用户认为数据集之间不应有任何缓存污染，每个数据集的地图应严格绑定各自的原始数据。

### 📦 用户提了什么
- "我读取历史数据（4490) ->转4524 -> 切回4490.地图变了，看起来是变成4524了？反正经纬度坐标显示在顶上一条线"
- "数据集之前完全独立，完全隔离，各自的地图也是。怎么搞的？"

### 💭 为什么这样想
用户在 013 任务中确立了 R1~R5 规则，其中 R3（地图坐标系不变性）明确要求"任何情况下，与数据对应的地图坐标系均不会发生变化"。用户在实际验证时发现切回原数据集后地图 projection 仍是转换后的，直接违反 R3。用户强调"完全独立隔离"是因为 013 任务的规则文档已将数据集-地图关系定义为深度绑定且不可变，任何跨数据集的缓存共享都违背这一设计理念。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| src/components/data/GisData.vue | 修改 | 移除 datasetActiveDataMap 定义、getDatasetActiveData、watch datasets 清理缓存、handleActiveDataChange 的缓存写入；模板 :data 从 getDatasetActiveData(entry.id, entry.data) 改为 entry.data；补充 R1/R3 注释 |
| docs/dev-tasks/911-遗留问题.md | 修改 | 追加 5.4 [✅ 已解决] 转换预览污染原数据集地图缓存 |
| docs/dev-tasks/013-坐标系规则整理/坐标系规则规范.md | 修改 | R2 实现位置更新为 :data="entry.data" 直接绑定；R3 实现位置补充 handleActiveDataChange 不写入缓存 + GisMapBase projection 初始化后不可变说明 |
| git commit 85b016d | 提交 | 3 files changed, 18 insertions(+), 35 deletions(-) |
| git push | 失败 | github.com:443 连接超时，需稍后重试 |

根因分析：
- `handleActiveDataChange` 在转换预览阶段把转换后数据（crs=4524）写入 `datasetActiveDataMap[原数据集A.id]`
- `addDataset` 创建新数据集 B 后未清理 A 的缓存
- 切回 A 时 `getDatasetActiveData` 返回污染的 4524 数据
- `GisMapBase` 的 projection 仅在 onMounted 初始化时读取，不响应动态变化
- 4524 投影坐标在 4490 经纬度地图上显示为"顶上一条线"

验证结果：
- `npm run typecheck`：exit code 0
- `npm run build`：exit code 0，✓ built in 57.07s

### 🔮 下一步去哪里
5.4 bug 已修复并本地提交（85b016d），待网络恢复后执行 git push。后续可能的方向：1) 用户实际验证 4490→转4524→切回4490 地图正确显示 4490；2) 评估转换预览时地图不更新（保持原始数据）是否符合预期，或是否需要在转换后立即创建新数据集以支持预览；3) 检查是否有其他场景依赖 datasetActiveDataMap 的移除而需要适配。
