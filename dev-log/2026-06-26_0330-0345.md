# 03:30 - 03:45 | 同源数据集切换不重建转换路径树（5.6）

### 🧭 用户在想什么
用户在确认 5.5 修复后，进一步提出转换路径树的交互优化诉求：转换路径树不应在数据集切换时消失或重建，同源数据集（转换产生的数据集链）应共享同一个树结构，切换时仅通过高亮节点表示当前活跃版本。用户强调"应该一直是树结构的"，表达了对树结构持久性和视觉稳定性的要求。

### 📦 用户提了什么
- "转换路径最好不要消失，高亮节点"
- "转换路径树不同数据集维持不变只是高亮位置表示。应该一直是树结构的"

### 💭 为什么这样想
用户在 5.5 修复后实际验证版本切换，发现切换数据集（A→B）时转换路径树会重建/闪烁。用户认为转换路径是同源数据集的共享历史记录，切换同源数据集不应破坏树结构，只需高亮变化。这与 R1 规则"地图随数据集切换"配合——地图切换但树保持，形成"树不动、地图动、高亮动"的清晰交互。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| src/components/data/GisDataTransformer.vue | 修改 | watch props.data 恢复逻辑前新增同源切换检查：若新数据集 id 已在 crsVersions 的 datasetId 中，仅更新该版本 data 与 activeVersionName（高亮），不重建树、不保存旧路径，直接 return |
| docs/dev-tasks/911-遗留问题.md | 修改 | 追加 5.6 [✅ 已解决] 同源数据集切换转换路径树重建（树消失） |
| docs/dev-tasks/013-坐标系规则整理/坐标系规则规范.md | 修改 | R1 实现位置补充 watch props.data 同源切换检查说明 |
| git commit 43f3f1f | 提交 | 3 files changed, 28 insertions(+), 3 deletions(-) |
| git push | 失败 | github.com:443 连接超时，需稍后重试 |

修复逻辑：
- watch props.data 触发时，先检查 `crsVersions.find(v => v.datasetId === currentId)`
- 若找到（同源切换）：更新 `existingVer.data = data`（保持 activeData 一致）、设置 `activeVersionName = existingVer.name`（高亮）、更新 `lastActiveId`、emit 后 return
- 若未找到（非同源切换）：走原有保存旧路径→恢复/重置新路径逻辑

验证结果：
- `npm run typecheck`：exit code 0
- `npm run build`：exit code 0，✓ built in 53.37s

### 🔮 下一步去哪里
5.6 bug 已修复并本地提交（43f3f1f），待网络恢复后执行 git push。后续可能的方向：1) 用户实际验证 A↔B 切换时转换路径树保持不变、仅高亮变化；2) 评估在 B（转换后数据集）上继续转换到第三坐标系时，转换路径树如何扩展（当前 reloadVersionsData 基于 originData 重新计算，可能与链式转换语义有冲突）；3) 检查非同源数据集切换时树恢复是否正确。
