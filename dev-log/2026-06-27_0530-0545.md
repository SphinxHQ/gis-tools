# 05:30 - 05:45 | 重新设计状态栏图标并统一历史数据卡片信息量

### 🧭 用户在想什么
用户关注图标的语义表达能力和移动端/桌面端的显示一致性。用户认为底部状态栏的图标不能很好地表达含义，要求重新设计。同时用户发现历史数据卡片在移动端和桌面端显示差异过大，强调"数据的必要信息都要统计展示"，只是"文字、图标、布局、格式"可以有差异，但"信息量不要减少"。

### 📦 用户提了什么
- "重新做一遍这些图标看"
- "datasource和dataset你都弄一个"
- "要素图标。底部状态栏里面的图标都不能很好的表达意思。你检查一下"
- "还有导出图标"
- "右侧侧边栏展开后的图标没对。项目中的数据源数据集图标没有完全对上。圆柱应该是数据源。表格应该是数据集。你把项目内涉及这些概念的地方都要统一一下"
- "移动和桌面的卡片一样的。这个需要调整"
- "数据的必要信息都要统计展示。只是文字、图标、布局、格式。这些有差异。但是信息量不要减少"

### 💭 为什么这样想
用户之前已经完成了几何类型图标语义色恢复、状态栏布局调整、历史数据tab恢复等工作。本次关注点从"功能恢复"转向"图标语义优化"和"响应式信息完整性"。用户希望图标能直观表达功能含义，而非抽象符号；同时希望移动端不因屏幕窄而丢失关键信息，保持与桌面端的信息对等。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| src/components/renders/TabIconRender.vue | 修改 | 重新设计5个状态栏图标：crs改为地球+经纬线、feature改为地图图钉、validate改为盾牌+勾号、export改为文档+向右箭头、vertex改为三角形+实心顶点节点 |
| src/components/data/HistoryDataList.vue | 修改 | 移动端卡片增加格式类型、文件大小、CRS、时间信息，与桌面端信息量一致；桌面端增加格式类型标签和文件大小列；新增formatFileSize和formatFileType工具函数 |
| src/components/data/GisData.vue | 修改 | 删除已迁移到HistoryDataList的无用代码：formatCount/formatTimeDate/formatTimeHM函数、GeoTypeIconRender/Common导入、约200行已迁移CSS样式 |

### 🔮 下一步去哪里
1. 用户可能需要查看实际图标视觉效果，确认新图标是否满足表达需求
2. 可能需要进一步微调图标SVG路径细节（如线条粗细、填充比例）
3. 可能需要清理IndexedDB中的测试数据
