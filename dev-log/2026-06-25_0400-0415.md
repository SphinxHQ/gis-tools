# 04:00 - 04:15 | 一期任务核实与5×6测试矩阵验证

### 🧭 用户在想什么
用户发现 AI 长时间停留在导入弹窗（GisDataReader）的 tab 排版细节上，提醒"你要不要核实一下一期任务？这个只是导入弹窗的内容"。用户思维从"细节修复"拉回到"一期任务核心验收"，要求所有验收点在5个size下测试、所有展示内容验证，验证方式按之前总结的检验规则矩阵执行。

### 📦 用户提了什么
- "等等。你要不要核实一下一期任务？这个只是导入弹窗的内容。"
- "所有验收点。都要有5个size测试，并且所有的展示内容都要验证。验证方式你刚刚也在不断总结更新"

### 💭 为什么这样想
一期任务核心是"布局统一与架构重构"，包含6个验收点（5级断点、单一DOM、4视口布局、顶栏高度、左面板显隐、拖拽手柄）。AI 之前在导入弹窗里修 tab 排版属于一期之外的额外修复，偏离了任务核心。用户通过提醒将 AI 拉回正轨，要求系统性验证而非零散修复。

### 🛠 做了什么
| 文件路径 | 操作 | 说明 |
|---|---|---|
| `src/composables/useBreakpoint.ts` | 修改 | PANEL_WIDTH.md 0→320，PANEL_COLLAPSED_WIDTH.md 0→48，修复平板端左面板不显示的违规 |
| `src/components/data/GisData.vue` | 修改 | 左面板CSS隐藏阈值 <1024px→<768px；状态栏mode从isMobile改为isDesktop（平板端用compact） |
| `src/components/data/GisDataOverview.vue` | 修改 | status-item flex-shrink 0→1 + min-width:0，允许坐标系项收缩，修复full模式溢出 |

### 🔮 下一步去哪里
一期任务30/30测试组合全部通过，可进入二期任务（Bottom Sheet + FAB）或提交一期成果。
