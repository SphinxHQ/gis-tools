# S6: 测试验证 — 面板修复

## 测试方案选定

- 部署形态: 本地部署（dev server `http://localhost:5176/gis-tools/`）
- 后端验证: N/A（纯前端项目）
- 前端验证: `mcp_playwright`（AI 直接驱动浏览器）
- 复杂场景: 默认不考虑

## 编译/静态验证

- 命令: `npm run build`
- 结果: **通过**（exit code 0，53.59s 完成）
- 产物: dist/ 目录正常生成，PWA sw.js + workbox 正常
- TypeScript 诊断: 0 errors（GetDiagnostics 验证）

## AI 自动测试

### 测试数据准备

- 数据来源: IndexedDB 历史记录
- 数据集: "绘制-1782233293009"
- CRS: EPSG:4490（CGCS2000 地理坐标系）
- 要素: 3 个 Polygon（面），12 个顶点

### 测试流程与结果

#### 步骤 1: 页面加载与空状态

- 操作: `browser_navigate` → `http://localhost:5176/gis-tools/`
- 结果: **通过** — 页面正常加载，显示空状态（"暂无数据 拖拽文件到此，或者点击查看更多方式"）

#### 步骤 2: 导入对话框

- 操作: 点击"导入"按钮 → 对话框打开 → 点击"历史记录"Tab → 点击数据行
- 结果: **通过** — 对话框自动关闭（`handleRead` → `addDataset` → `dialogVisible = false`），数据集成功添加

#### 步骤 3: 数据源 Tab（M1 + M2 验证）

- 操作: 默认激活数据源 Tab
- 结果: **通过**
- 证据:
  - el-segmented 切换器显示 5 个选项（数据源/坐标系/要素/校验/导出）
  - 双层结构正常渲染：
    - 第一层「数据导入」: GisDataReader（上传文件/输入文本/绘制图形/历史记录 4 个子 Tab）
    - 第二层「数据集 (1)」: 数据集卡片显示
      - 名称: "绘制-1782233293009"
      - CRS 标签: EPSG:4490
      - 统计: 3 要素, 12 顶点
      - 删除按钮可用
      - 激活态高亮（border + background）
- 截图: `s6-source-tab-double-layer.png`

#### 步骤 4: 坐标系 Tab（含 TDZ 修复验证）

- 操作: 点击"坐标系"Tab
- 结果: **通过**（TDZ 修复生效）
- 证据:
  - GisDataTransformer 正常渲染，无 `ReferenceError`
  - 当前坐标系卡片: EPSG:4490
  - 名称: China Geodetic Coordinate System 2000
  - 类型: 地理坐标系
  - 中央经线: 104.19500000000001°
  - 带号: -1
  - 坐标转换按钮可用
  - 重设坐标系按钮可用
- **关键**: 修复前此 Tab 报 `Cannot access 'emitActiveDataChange' before initialization`，修复后完全正常

#### 步骤 5: 要素 Tab

- 操作: 点击"要素"Tab
- 结果: **通过**
- 证据:
  - GisFeatureTree 正常渲染
  - 视图切换器: 结构树（激活）/ 要素编辑
  - 结构树显示:
    - 根节点: "几何要素 3 项"（展开）
    - 子节点 1: "要素 面" + 编辑JSON按钮
    - 子节点 2: "要素 面" + 编辑JSON按钮
    - 子节点 3: "要素 面" + 编辑JSON按钮

#### 步骤 6: 校验 Tab

- 操作: 点击"校验"Tab
- 结果: **通过**
- 证据:
  - GisDataValidator 正常渲染
  - "执行校验"按钮可用
  - 结果树区域显示空状态: "点击「执行校验」检查数据"

#### 步骤 7: 导出 Tab

- 操作: 点击"导出"Tab
- 结果: **通过**
- 证据:
  - GisDataExport 正常渲染
  - 格式切换器: GeoJson（激活）/ WKT / 电子报盘 — 三格式合并
  - 导出模式: FeatureCollection（激活）/ FeatureList Array / Feature Split
  - 选项: 包含 CRS 复选框、格式化下拉
  - 下载按钮: 下载 JSON、下载 Shapefile
  - 代码预览区: 显示完整 GeoJson 内容（含实际坐标数据）
- 截图: `s6-export-tab.png`

## 逐项验证

### M1: el-tabs → el-segmented + v-show

- 验证方法: Playwright 点击 5 个 Tab，验证每个 Tab 内容切换正常
- 结果: **通过** — 5 个 Tab 均可切换，内容正确显示，无 el-tabs 嵌套冲突

### M2: 数据源 Tab 双层结构

- 验证方法: Playwright 快照验证 DOM 结构
- 结果: **通过** — 数据导入区 + 数据集卡片列表双层结构正常，数据集卡片信息完整

### M3: 样式适配

- 验证方法: Playwright 截图视觉验证
- 结果: **通过** — el-segmented 全宽显示，数据集卡片样式正常，激活态高亮

### D1（偏差）: GisDataTransformer TDZ 修复

- 验证方法: Playwright 点击坐标系 Tab，检查控制台错误
- 结果: **通过** — 无 `ReferenceError`，GisDataTransformer 正常渲染 CRS 信息

## 待用户确认项

无。所有验证项均由 AI 通过 Playwright 自动验证完成。

## 风险接受记录

无跳过项。

## 结论: **通过**

- 编译验证: 通过
- 5 个 Tab 功能验证: 全部通过
- TDZ 修复验证: 通过
- 双层结构验证: 通过
- 截图证据: 2 张（数据源 Tab、导出 Tab）
