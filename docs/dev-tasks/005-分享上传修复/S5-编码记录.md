# S5: 编码实施 — 分享上传修复

## 实施进度

- [x] M1: el-upload 添加 accept 属性 — 已完成
- [x] M2: manifest 显式补充 start_url 和 scope — 已完成
- [x] M3: pwa-icon.svg 视觉参数调整 — 已完成

## 实施详情

### M1: el-upload 添加 accept 属性
- 文件: `src/components/data/GisDataReader.vue:232`
- 改动: 在 el-upload 添加 accept 属性，覆盖解析器支持的全部格式后缀和 MIME 类型
- accept 值: `.geojson,.json,.wkt,.txt,.csv,.shp,.zip,.dxf,.exf,.wkb,application/json,application/zip,application/octet-stream,text/plain,text/csv,application/dxf`

### M2: manifest 显式补充 start_url 和 scope
- 文件: `vite.config.ts:169-170`
- 改动: 在 manifest 对象 display 字段后添加 `start_url: '/gis-tools/'` 和 `scope: '/gis-tools/'`
- 附带注释说明：已安装 PWA 缓存旧 manifest，需重装才能注册 share_target

### M3: pwa-icon.svg 视觉参数调整
- 文件: `public/pwa-icon.svg`
- 改动:
  - stroke-width: 16 → 21（+30%）
  - 5 个 circle r: 20 → 26（+30%）
  - fill rgba alpha: 0.15 → 0.45（+0.30 绝对值）

## 偏差记录
无偏差。实际改动与 S3 计划完全一致。

## S5R 跳过原因
本次为轻量任务（3 处单点修改，无逻辑变更），由 AI 自评后跳过 S5R 直接进入 S6。
AI 自评要点：
- M1: accept 属性值覆盖 GisDataType 全部格式，不改变 handleFileChanged 逻辑
- M2: start_url/scope 值与 VitePWA 自动补充值一致，零风险
- M3: 纯数值调整，SVG 尺寸和 viewBox 不变
- 三项修改相互独立，无跨模块影响
