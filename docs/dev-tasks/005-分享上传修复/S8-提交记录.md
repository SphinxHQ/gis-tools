# S8: 提交记录 — 分享上传修复

## 版本控制
- 版本控制系统: git
- 提交所在分支: `feature/share-target`
- 是否在主线: 否

## 提交标识
- 提交哈希: 813e21d

## 提交信息
```
fix: 修复移动端上传文件过滤、补充PWA分享配置、调整图标视觉

- GisDataReader.vue: el-upload 添加 accept 属性，覆盖 GeoJSON/WKT/SHP/ZIP/DXF/EXF 等全部空间数据格式，解决移动端文件选择器只显示媒体文件的问题
- vite.config.ts: manifest 显式补充 start_url 和 scope，与 share_target.action 路径保持一致（已安装 PWA 需重装才能注册分享目标）
- pwa-icon.svg: 线宽 16→21(+30%)、节点半径 20→26(+30%)、中间填充不透明度 0.15→0.45(+0.30)
```

## 变更文件清单
| 文件路径 | 操作 | 说明 |
|---|---|---|
| `public/pwa-icon.svg` | 修改 | 线宽/半径/不透明度 +30% |
| `src/components/data/GisDataReader.vue` | 修改 | el-upload 添加 accept 属性 |
| `vite.config.ts` | 修改 | manifest 补充 start_url 和 scope |

## 推送结果
- 推送状态: **失败（网络问题）**
- 失败原因: `Failed to connect to github.com port 443` / `Connection was reset`
- 本地提交: 已完成（813e21d）
- 后续操作: 待网络恢复后执行 `git push origin feature/share-target`

## 备注
- `docs/dev-tasks/005-分享上传修复/` 目录被 `.gitignore` 忽略（`docs/` 规则），任务文档仅保留在本地，不纳入提交
- 移动端真机验证待用户完成：
  1. 安卓手机安装 PWA 后点击"上传文件"，确认文件选择器出现"文件"分类
  2. 卸载旧 PWA → 重新安装 → 从其他 App 分享文件，确认系统分享面板出现 "GIS Tools"
  3. 浏览器打开 pwa-icon.svg 确认视觉调整效果
