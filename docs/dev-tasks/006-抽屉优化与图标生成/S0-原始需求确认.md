# S0: 原始需求确认 — 移动端抽屉优化与图标生成

## 用户原始需求

用户提出三个需求，要求合并为一个新任务推进：

**需求1（移动端底部抽屉优化）**：
> "调整一下移动端的底部抽屉，增加一些高度，系统性的大幅降低 抽屉容器再移动端下的各种留白"

**需求2（PWA 图标生成）**：
> "使用 scripts 里面的 gen-pwa-icons.mjs 生成 app 图片"

**需求3（合并到主干）**：
> "合并到主干"

## 任务文档根目录确认
- 默认建议: `docs/dev-tasks/`
- 用户确认路径: `docs/dev-tasks/`（沿用既有约定）

## 历史任务检查
- 是否找到相似任务: 否
- 处理方式: 新建任务（006）

## 代码初步定位

### 需求1：移动端底部抽屉
- 相关文件:
  - `src/components/data/GisMobileNav.vue:86-132`（el-drawer direction="btt" size="35%"）
  - `src/components/data/GisMobileNav.vue:155-213`（抽屉样式：drawer-handle margin 8px/4px、drawer-body padding 2px、mobile-tab-bar height 56px）
  - `src/App.vue:117-152`（全局移动端弹窗留白优化，但未覆盖 el-drawer）
- 初步观察:
  - 抽屉高度 size="35%" 偏低，用户要求增加高度
  - 抽屉内多处留白：drawer-handle 上下 margin、drawer-body padding、el-drawer 默认 body padding
  - App.vue 的移动端优化只针对 el-dialog，未覆盖 el-drawer

### 需求2：PWA 图标生成
- 相关文件: `scripts/gen-pwa-icons.mjs`
- 初步观察: 脚本使用 sharp 库读取 `public/pwa-icon.svg` 生成 192x192 和 512x512 PNG。005 任务已更新 SVG 源文件（线宽/半径/不透明度 +30%），需运行脚本重新生成 PNG

### 需求3：合并到主干
- 当前分支: `feature/share-target`（含 004 分享接收 + 005 修复，共 2 个提交未合并到 main）
- 远端状态: 004 已推送，005 本地提交 813e21d 因网络未推送
- 合并方式: 待确认（直接 merge / rebase / PR）

## AI 复述后的需求

本次任务包含三个需求：

1. **移动端底部抽屉优化**：
   - 增加抽屉高度（当前 35% → 提高到 50% 左右）
   - 系统性降低抽屉容器在移动端下的各种留白（drawer-handle margin、drawer-body padding、el-drawer 默认 padding 等）

2. **PWA 图标生成**：运行 `scripts/gen-pwa-icons.mjs`，基于 005 更新后的 SVG 源文件重新生成 pwa-192x192.png 和 pwa-512x512.png

3. **合并到主干**：将 feature/share-target 分支合并到 main

## 需求边界
- 本次包含:
  - `GisMobileNav.vue` 抽屉高度和留白调整
  - `App.vue` 移动端 el-drawer 全局留白优化（如需要）
  - 运行 gen-pwa-icons.mjs 生成 PNG
  - feature/share-target 合并到 main
- 本次不包含:
  - 不重构抽屉组件结构
  - 不修改抽屉内的子组件（GisDataTransformer/GisFeatureTree 等）
  - 不修改 gen-pwa-icons.mjs 脚本本身

## 用户确认记录
- 结论: 已确认
- 用户补充:
  1. 抽屉高度：50%
  2. 合并方式：本地 merge + push main

## 版本控制
- 版本控制系统: git
- 当前状态:
  - 当前分支: `feature/share-target`
  - 未提交改动: 无（005 已提交 813e21d，未推送）
  - 与远端差异: 领先 1 个提交（813e21d 未推送）
  - 最近提交摘要: `fix: 修复移动端上传文件过滤、补充PWA分享配置、调整图标视觉`
- AI 建议: 继续在 `feature/share-target` 分支完成需求1和需求2，然后推送，最后本地 merge 到 main 并 push main
- 用户选择: 继续在 feature/share-target 分支完成开发 → 本地 merge 到 main → push main
- 最终采用: feature/share-target 开发 → merge → main

## 流程配置
- 技术栈: Vue 3 + TypeScript + Vite + Element Plus + vite-plugin-pwa + sharp
- 启用验收确认 S7: 否
- 启用编译验证: 是（npm run build）
- 启用推送: 是
- 改动评估: 是
