# S3: 实施计划 — 移动端抽屉优化与图标生成

## 修改项清单

### M1: 抽屉高度提升 + 组件内留白缩减
- 文件: `src/components/data/GisMobileNav.vue`
- 修改内容:
  1. `:89` size="35%" → size="50%"
  2. `:172` drawer-handle margin: 8px auto 4px → margin: 4px auto 2px（上下留白 12px→6px）
  3. `:180` drawer-body padding: 2px → padding: 0（移除冗余 padding）
- 影响面: 仅移动端底部抽屉，桌面端不使用此组件
- 验证方法: 构建通过 + 移动端视觉确认（待用户）
- 回滚方式: 恢复原数值

### M2: 全局 el-drawer 移动端留白优化
- 文件: `src/App.vue:117-152`（@media max-width:767px 区块内追加）
- 修改内容: 追加 el-drawer 移动端留白优化样式
  ```css
  .el-drawer__body {
    padding: 8px 8px 4px !important;
  }
  .el-drawer__header {
    padding: 8px 12px !important;
    margin-bottom: 0 !important;
  }
  ```
- 影响面: 所有移动端 el-drawer（GisMobileNav 底部抽屉 + MapDrawer 侧边抽屉 + GisData 数据源抽屉），统一缩减留白
- 验证方法: 构建通过 + 移动端各抽屉视觉确认（待用户）
- 回滚方式: 删除追加的样式

### M3: 生成 PWA 图标 PNG
- 文件: `public/pwa-192x192.png`、`public/pwa-512x512.png`（二进制更新）
- 修改内容: 运行 `node scripts/gen-pwa-icons.mjs`，基于 005 更新后的 SVG 重新生成 PNG
- 影响面: PWA 安装图标
- 验证方法: 脚本执行成功 + PNG 文件更新时间戳确认
- 回滚方式: git checkout 恢复旧 PNG

### M4: 合并到主干
- 操作: git checkout main → git merge feature/share-target → git push origin main
- 影响面: main 分支
- 验证方法: git log 确认合并结果 + push 成功
- 回滚方式: git reset --hard 恢复 main（需用户确认）

## 改动评估

| 修改项 | 复用 | 风险 | 影响 | 加权总分 | 结论 | 备注 |
|--------|------|------|------|----------|------|------|
| M1 | 95 | 90 | 92 | 92 | 推荐实施 | 纯数值调整 |
| M2 | 92 | 88 | 90 | 90 | 推荐实施 | 复用 App.vue 已有模式 |
| M3 | 100 | 95 | 95 | 97 | 推荐实施 | 运行既有脚本 |
| M4 | 100 | 85 | 95 | 93 | 推荐实施 | 标准 git 操作 |

## 执行顺序
1. M1（抽屉高度+留白）→ 2. M2（全局 el-drawer 留白）→ 3. M3（生成图标）→ 4. 构建验证 → 5. 提交推送 → 6. M4（合并到 main）

## 风险与回滚总述
- M1/M2 相互关联（都涉及移动端抽屉留白），M1 改组件内，M2 改全局，互补不冲突
- M3 依赖 sharp 库，如未安装需先 npm install sharp
- M4 合并前需确保 feature/share-target 已推送（005 的 813e21d 因网络未推送）

## 微创自检
- 是否落在 S1 最小改动假设内: 是（2 代码文件 + 2 二进制文件 + git 操作）
- 是否存在更小实现路径: 否
- 新增依赖: 无（sharp 应已在 devDependencies，待 S5 确认）
- 是否绕开现有能力: 否（M2 复用 App.vue 模式，M3 复用脚本）
- 是否修改公开接口/数据结构/配置: 否
- 是否完整解决 S1 问题: 是
- 自检结论: 通过
