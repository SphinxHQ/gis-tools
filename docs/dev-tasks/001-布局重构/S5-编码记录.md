# S5: 编码实施 — 布局重构

## 实施进度

- [x] M1: useBreakpoint composable — 已完成
- [x] M5: GisDataTransformer 去 tabs 化 — 已完成
- [x] M8: MapDrawer 抽屉化 — 已完成
- [x] M6: GisDataViewer 拆分（GisDataOverview + GisDataExport）— 已完成
- [x] M7: GisDataInspactor 拆分（GisFeatureTree + GisDataValidator + useMapController）— 已完成
- [x] M3: GisDataPanel 新建（5 Tab 容器）— 已完成
- [x] M4: GisMobileNav 新建（移动端底部导航 + 上滑面板）— 已完成
- [x] M2: GisData.vue 重构（顶栏 + 可折叠左面板 + 主内容区）— 已完成
- [x] M9: 废弃 GisDataSetTabs（清理旧组件引用）— 已完成

## 实施详情

### M1: useBreakpoint composable
- 文件: `src/composables/useBreakpoint.ts`（新建）
- 内容: 4 档断点响应式工具（xl≥1280/lg=1024-1279/md=768-1023/sm<768）
- 导出: current, isMobile, isTablet, isDesktop, panelWidth, panelCollapsedWidth, showLeftPanel, showMobileNav
- 模式: 全局单例 + 引用计数 + resize 监听

### M5: GisDataTransformer 去 tabs 化
- 文件: `src/components/data/GisDataTransformer.vue`（重写）
- 变更: 移除 el-tabs，改为 CRS 信息卡片 + 转换链面包屑 + el-select 版本切换 + 操作按钮
- 关键: emit `active-data-change` 事件 `(data, transformChain)` 替代原内部包含 GisDataViewer

### M8: MapDrawer 抽屉化
- 文件: `src/components/data/MapDrawer.vue`（重写）
- 变更: 桌面端=可折叠侧边栏（200px/48px），移动端=el-drawer direction="ltr" + 触发按钮
- 使用 useBreakpoint() 检测 isMobile

### M6: GisDataViewer 拆分
- 新建 `src/components/data/GisDataOverview.vue`: 数据信息卡片（主内容区）
- 新建 `src/components/data/GisDataExport.vue`: 合并 GeoJson/Wkt/电子报盘 导出（el-segmented 切换）
- 原 GisDataViewer.vue 已删除

### M7: GisDataInspactor 拆分
- 新建 `src/composables/useMapController.ts`: 地图实例管理 composable
- 新建 `src/components/data/GisFeatureTree.vue`: 结构树 + 节点 JSON 编辑 + 要素编辑器
- 新建 `src/components/data/GisDataValidator.vue`: 校验树 + 执行按钮
- 原 GisDataInspactor.vue 已删除

### M3: GisDataPanel 新建
- 文件: `src/components/data/GisDataPanel.vue`（新建）
- 内容: 左面板 5 Tab 容器（数据源/坐标系/要素/校验/导出）
- 集成: GisDataReader, GisDataTransformer, GisFeatureTree, GisDataValidator, GisDataExport

### M4: GisMobileNav 新建
- 文件: `src/components/data/GisMobileNav.vue`（新建）
- 内容: 移动端底部 Tab 导航 + el-drawer direction="btt" 上滑面板
- 5 个底部 Tab: 数据源/坐标系/要素/校验/导出

### M2: GisData.vue 重构
- 文件: `src/components/data/GisData.vue`（重写）
- 变更: 从 "toolbar + tabs" 改为 "顶栏 + 左面板 + 主内容区"
- 顶栏: logo + el-select 数据集下拉 + 导入按钮 + 主题切换
- 桌面端: 可折叠左面板（GisDataPanel）+ 主内容区（GisDataOverview + GisMapTianditu）
- 移动端: GisDataOverview + 全屏地图 + GisMobileNav 底部导航
- 使用 useMapController composable 管理地图实例

### M9: 废弃 GisDataSetTabs
- 删除文件:
  - `src/components/data/GisDataSetTabs.vue`
  - `src/components/data/GisDataViewer.vue`
  - `src/components/data/GisDataInspactor.vue`
  - `src/components/data/GisDataToolbar.vue`
- `src/components.d.ts` 为自动生成文件，构建时自动更新

## 偏差记录

### 偏差1: GisData.vue 图标导入缺失
- 偏差: 初版 GisData.vue 模板使用了 Monitor/Sunny/Moon 图标但未在 import 中声明
- 处理: 已在 import 中补充 Monitor, Sunny, Moon 图标导入

### 偏差2: GisMobileNav.vue import 位置不规范
- 偏差: `import { watch } from 'vue'` 写在脚本中间
- 处理: 已合并到文件顶部的 import 语句中

## 构建验证

- 命令: `npm run build`
- 结果: 通过（exit code 0，51.34s 完成）
- 产物: dist/ 目录正常生成，含 GisData chunk (4713.80 kB)
