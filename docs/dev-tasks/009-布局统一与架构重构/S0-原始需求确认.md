# S0: 原始需求确认 — 布局统一与架构重构

## 用户原始需求

> 装载 `c:\Users\YuanYu\Desktop\git\gis-tools\.agents\geo-compacter.md` 作为强化记忆的上下文，按 `c:\Users\YuanYu\Desktop\git\gis-tools\dev-log\temp\ux-renovation-plan.md` 设计的一期改造方案进行推进

## 任务文档根目录确认

- 默认建议: `docs/dev-tasks/`
- 用户确认路径: `docs/dev-tasks/`

## 历史任务检查

- 是否找到相似任务: 是
- 相似任务路径: `docs/dev-tasks/001-布局重构/`
- 相似任务状态: 已完成（存在 S8-提交记录.md）
- 处理方式: 新建任务（001 已完成，本次按新任务 009 执行）

## 代码初步定位

### 强化记忆装载

已完整读取 `.agents/geo-compacter.md`（434 行），核心规范摘要：

- **单一代码路径原则**：移动端与桌面端业务逻辑共享同一套代码，禁止 `if (isMobile)` 分支业务逻辑
- **5 级断点体系**：`xs(<576) / sm(576-767) / md(768-1023) / lg(1024-1279) / xl(>=1280)`
- **硬性约束**：禁止 `v-if` 硬切换两套布局结构，改用 CSS `@media` + flexbox/grid 渐进适配
- **顶栏高度**：移动端 48px + safe-area-inset-top，桌面端 40px

### 一期改造方案（来自 ux-renovation-plan.md）

| # | 违规项 | 位置 | 严重度 |
|---|--------|------|--------|
| 1.1 | 双套布局硬切换：`v-if="showLeftPanel"` 在 `desktop-layout` 与 `mobile-layout` 间切换 | `GisData.vue` L390-430 | 🔴 |
| 1.2 | 断点体系缺失 xs 级：仅 4 级断点 `sm/md/lg/xl`，无 `<576px` 的 `xs` | `useBreakpoint.ts` L55-58 | 🟡 |
| 1.3 | 移动端顶栏高度反直觉：`.is-mobile .top-bar { height: 36px }` 比桌面 40px 更矮 | `GisData.vue` L679-683 | 🟡 |
| 1.4 | 导入弹窗未适配响应式：`width="96%"` 在所有断点相同 | `GisDataImportDialog.vue` L60-61 | 🟢 |

### 相关文件

| 文件 | 当前状态 |
|------|----------|
| `src/composables/useBreakpoint.ts` | 4 级断点（sm/md/lg/xl），`isMobile = sm`，`showLeftPanel = panelWidth > 0` |
| `src/components/data/GisData.vue` | `v-if="showLeftPanel"` 切换 desktop-layout / mobile-layout 两套 DOM |
| `src/components/data/GisDataImportDialog.vue` | `el-dialog width="96%"` 无响应式适配 |

## AI 复述后的需求

按 `ux-renovation-plan.md` 第一期方案，完成以下改造：

1. **useBreakpoint.ts**：新增 `xs` 断点（<576px），建立 5 级断点体系；`isMobile` 覆盖 `xs + sm`；新增 `showBottomNav`（<768px 为 true）
2. **GisData.vue**：移除 `v-if="showLeftPanel"` 双套布局，统一为单一弹性布局 DOM 结构；左面板通过 CSS `@media` 控制显隐；顶栏高度按断点适配（桌面 40px / 平板 44px / 移动 48px）
3. **GisDataImportDialog.vue**：弹窗宽度添加 `max-width: 640px` @media ≥768px

## 需求边界

### 本次包含

- `useBreakpoint.ts` 断点体系扩展（4 级 → 5 级）
- `GisData.vue` 模板结构统一（双套 DOM → 单套弹性布局）
- `GisData.vue` 顶栏高度响应式适配
- `GisDataImportDialog.vue` 弹窗宽度响应式适配
- 移动端底部导航（GisMobileNav）在新布局中的位置调整

### 本次不包含

- Bottom Sheet 组件新建（第二期）
- FAB 悬浮按钮（第二期）
- 手势增强与触控 resize（第三期）
- 设计 Token 化（第四期）
- 死代码清理与目录重构（第五期）
- GisMobileNav 内部逻辑改造（第二期）

## 用户确认记录

- 结论: 已确认
- 用户补充: 先将 main 提交推送，然后再开始新建分支进行任务

## 版本控制

- 版本控制系统: git
- 当前状态:
  - 当前分支: `feature/phase1-layout-unify`（从 main 新建）
  - main 分支已提交：`46f415e chore: 提交main分支累积改动`
  - main 推送状态: 失败（网络连接重置），本地提交已完成
  - 最近提交摘要: `46f415e` (feature/phase1-layout-unify) / `1dec62b` (origin/main)
- AI 建议: 在 feature/phase1-layout-unify 分支上进行开发，完成后合并回 main
- 用户选择: 新建 feature 分支（已执行）
- 最终采用: `feature/phase1-layout-unify` 分支

## 流程配置

- 技术栈: Vue 3 + TypeScript + Vite + Element Plus + OpenLayers
- 启用验收确认 S7: 是
- 启用编译验证: 是（`npm run build`）
- 启用推送: 是（main 推送待网络恢复后重试，feature 分支完成后推送）
- 改动评估: 否
