# 06-防返工自检清单

> 来源：[910-经验教训.md](../910-经验教训.md) 一二两期返工复盘提炼
> 用途：测试执行前后逐项核对，防止返工
> 使用：每项测试完成后，对照本清单做最终自检

---

## 一、方案设计阶段自检

> 引入新组件 / 新功能前的场景适用性检查

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| D-001 | 场景价值自检 | 新组件在该场景下是否提供不可替代的用户价值？ | | FAB 移除教训 |
| D-002 | 功能冗余自检 | 现有组件是否已覆盖该功能？新组件是否造成重复？ | | MapControlPanel 已有绘制工具 |
| D-003 | 风格一致性自检 | 新组件风格是否与整体设计语言一致？ | | FAB Material vs Element Plus |
| D-004 | 用户路径自检 | 用户在该场景下的核心任务是什么？新组件是否服务于核心任务？ | | |
| D-005 | 推迟项归属 | 推迟项是否明确归属期次？ | | 避免遗漏 |

---

## 二、执行实施阶段自检

### 2.1 合并完整性（分支合并）

| 编号 | 自检项 | 命令 / 方法 | 通过 | 备注 |
|---|---|---|---|---|
| E-001 | 合并前 diff 验证 | `git diff <target>..<source> --stat` 输出预期文件清单 | | |
| E-002 | 合并后 commit 验证 | `git log --oneline <target> -5` 确认合并 commit 存在 | | |
| E-003 | 合并后改动核对 | `git diff <target>~1..<target> --stat` 改动文件数符合预期 | | |
| E-004 | 合并后编译验证 | `npx vue-tsc --noEmit` 通过 | | |
| E-005 | 合并后启动验证 | dev server 正常启动 | | |
| E-006 | 合并策略明确 | 使用 `--no-ff` 保留合并记录 | | |

### 2.2 Flex 布局陷阱

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| E-101 | Flex 子项显式约束 | 每个 flex 子项是否有显式 `flex` 简写或 `flex-grow/shrink/basis`？ | | |
| E-102 | 固定高度区约束 | 顶栏/底栏/Tab 栏是否 `flex-shrink: 0` + `overflow: hidden`？ | | |
| E-103 | border-box 默认化 | 所有布局容器是否 `box-sizing: border-box`？ | | |
| E-104 | flex-basis 方向一致性 | 嵌套 flex 容器中 `flex-basis` 百分比方向是否与父容器 `flex-direction` 一致？ | | 优先用 `width: 100%` |
| E-105 | 5 断点动态验证 | 是否在 5 断点动态验证布局无溢出？ | | |

### 2.3 方案实施对照

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| E-201 | 组件变更表对照 | 是否对照方案组件变更表逐项核对？ | | GisMobileNav 改名遗漏 |
| E-202 | 偏离方案记录 | 任何偏离方案的决策是否在文档中说明原因？ | | |

---

## 三、验证阶段自检

### 3.1 5 断点全覆盖（响应式改造必做）

| 编号 | 自检项 | 视口 | 通过 | 备注 |
|---|---|---|---|---|
| V-001 | xs 动态验证 | 375x667 | | topBar/content/navArea 边界 = 视口边界 |
| V-002 | sm 动态验证 | 600x960 | | 同上 |
| V-003 | md 动态验证 | 768x1024 | | 左面板显示/隐藏切换正确 |
| V-004 | lg 动态验证 | 1024x768 | | 左面板宽度 280px |
| V-005 | xl 动态验证 | 1280x800 | | 左面板宽度 320px |
| V-006 | 每断点 boundingRect | 5 断点 | | 输出 topBar/content/map/navArea 的 getBoundingClientRect |
| V-007 | 移动端优先验证 | xs/sm | | 移动端断点是溢出高发区，优先验证 |
| V-008 | 验证矩阵文档化 | — | | 输出 5 断点验证矩阵表 |

### 3.2 真机依赖项标注

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| V-101 | TouchEvent 标注 | MCP 无法验证的触摸项是否标注"待真机"？ | | |
| V-102 | 拖拽手势标注 | 滑动/拖拽/长按手势是否标注"待真机"？ | | |
| V-103 | 性能感知标注 | 性能受设备限制的项是否标注"待真机性能验证"？ | | |

### 3.3 控制台错误检查

| 编号 | 自检项 | 通过 | 备注 |
|---|---|---|---|
| V-201 | error 数量 = 0 | | 新增 error 阻塞 |
| V-202 | 警告区分新增/预存在 | | 仅新增警告判断 |
| V-203 | 基线警告清单建立 | | 参考已知预存在警告 |

---

## 四、跨平台阶段自检（Windows 环境）

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| C-001 | 跨平台命令适配 | 是否避免 Bash 专属语法（heredoc 等）？ | | PowerShell 不支持 |
| C-002 | 多行 commit message | 是否使用 `git commit -F <file>` + 临时文件方式？ | | |
| C-003 | 路径分隔符 | 是否使用 `/` 或确保 PowerShell 兼容？ | | |
| C-004 | 引号转义 | PowerShell 双引号转义规则是否正确？ | | |

---

## 五、MCP 工具使用自检

| 编号 | 自检项 | 自检问题 | 通过 | 备注 |
|---|---|---|---|---|
| M-001 | 文件上传路径 | 测试文件是否在 `C:\Users\YuanYu\.playwright-mcp\` 允许路径？ | | |
| M-002 | 视口实测值 | 是否以实测视口为准并标注 DPR 缩放？ | | 设置值 ≠ 实测值 |
| M-003 | 元素 uid 刷新 | click 失败时是否用 `take_snapshot` 重新获取 uid？ | | uid 会变化 |
| M-004 | 工具连接断开 | 是否切换 chrome-devtools ↔ playwright？ | | 不重复重试 |
| M-005 | 控制台警告基线 | 是否在 S0 建立基线警告清单？ | | 避免误判 |

---

## 六、自检结论模板

```
防返工自检结论: ✅ 全部通过 / ⚠️ 有未通过项
- 方案设计: ✅ / ❌（未通过项: D-XXX）
- 执行实施: ✅ / ❌（未通过项: E-XXX）
- 验证阶段: ✅ / ❌（未通过项: V-XXX）
- 跨平台: ✅ / ❌（未通过项: C-XXX）
- MCP 工具: ✅ / ❌（未通过项: M-XXX）

未通过项处理: {说明}
```
