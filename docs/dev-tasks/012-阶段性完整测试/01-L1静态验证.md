# 01-L1 静态验证测试清单

> 层级：L1（静态验证）
> 通过标准：退出码 0，无新增错误
> 参考：[912 §三 L1 静态验证](../912-MCP自动化测试方法论.md#三l1-静态验证标准动作)

---

## 一、TypeScript 编译验证

| 编号 | 测试项 | 命令 | 预期结果 | 实测结果 | 备注 |
|---|---|---|---|---|---|
| L1-001 | Vue TypeScript 编译 | `npx vue-tsc --noEmit` | 退出码 0 | | |
| L1-002 | 区分新增/预存在错误 | 对比基线错误清单 | 新增错误数 = 0 | | 预存在错误需标注 |
| L1-003 | 严格类型检查 | 检查 `any` 类型使用 | 无 `any` 类型（除明确标注） | | 项目硬约束 |

### 已知预存在错误基线

> 参考 [911 §遗留问题](../911-遗留问题.md)，以下错误为预存在，非本次引入：

- `instanceId prop type Number vs String`（一期引入，警告级别）

---

## 二、构建验证

| 编号 | 测试项 | 命令 | 预期结果 | 实测结果 | 备注 |
|---|---|---|---|---|---|
| L1-101 | Vite 生产构建 | `npx vite build` | 退出码 0 | | |
| L1-102 | 构建产物完整性 | 检查 `dist/` 目录 | 生成 index.html + assets | | |
| L1-103 | 构建时长监控 | 记录构建耗时 | < 60s（基线参考） | | 超时需排查 |
| L1-104 | 产物大小监控 | 检查 chunks 大小 | 单 chunk < 1500 kB 警告属预存在 | | 记录基线 |
| L1-105 | Web Worker 打包 | 检查 worker 产物 | `dataParser.worker.js` 正常生成 | | 三期 011 引入 |

---

## 三、VS Code 诊断（补充验证）

| 编号 | 测试项 | 工具 | 预期结果 | 实测结果 | 备注 |
|---|---|---|---|---|---|
| L1-201 | LSP 诊断获取 | `GetDiagnostics` | 与 vue-tsc 结果一致 | | 可选交叉验证 |
| L1-202 | ESLint 诊断 | `GetDiagnostics` | 无 error 级别违规 | | 参考 SARIF 报告 |
| L1-203 | 未使用符号检查 | `GetDiagnostics` | 无新增 unused 警告 | | 012 报告基线 |

### SARIF 报告基线（参考）

> 来自 `dev-log/report_2026-06-25_12-38-13.sarif.json`：

- ESLint 违规：132 项（基线）
- 未使用全局符号：104 项（基线）
- 未使用 CSS 选择器：66 项（基线）
- 未使用 imports：21 项（基线）

> **判定规则**：本次测试仅对"新增"问题做阻塞判断，基线问题记录但不阻塞。

---

## 四、静态验证记录模板

```markdown
## L1 静态验证结果

- 命令: npx vue-tsc --noEmit
  - 结果: 通过 / 失败（X 个错误）
  - 其中新增错误: Y 个（列表）
  - 其中预存在错误: Z 个（基线对照）

- 命令: npx vite build
  - 结果: 通过 / 失败
  - 耗时: XXs
  - 产物大小: XXX kB

- VS Code 诊断:
  - 与 vue-tsc 一致: 是 / 否
  - 新增 ESLint 违规: X 项
```

---

## 五、L1 验证结论模板

```
L1 结论: ✅ 通过 / ❌ 失败
- TypeScript 编译: ✅ / ❌
- 构建验证: ✅ / ❌
- 诊断一致性: ✅ / ❌

阻塞项: 无 / {列表}
风险接受: {如有}
```
