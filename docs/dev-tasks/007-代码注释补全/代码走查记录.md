# 代码走查记录 — 007 代码注释补全

> 本文档记录在为代码添加 JSDoc 注释过程中发现的代码问题（逻辑错误、潜在 bug、不规范写法等）。
> 注释编写完成后，统一处理此处记录的问题。

## 走查发现

（随注释进度持续更新）

---

## 未使用链分析

> 以下导出/组件/类仅在自身文件或自动生成的 components.d.ts 中被引用，无任何业务代码实际使用。

### 完全未使用的模块（0 业务引用）

| 文件 | 导出项 | 说明 |
|---|---|---|
| `src/common/GisAdvancedAnalysis.ts` | `default export` (整个模块) | 1363 行高级分析工具类，无任何业务代码引用。包含 batchTransformCoordinates、editGeometryVertex、splitPolygonByLine、mergeAdjacentPolygons、queryFeaturesBySpatial、queryFeaturesByBBox、calculateGeometryStatistics、calculateAttributeStatistics、checkTopology、detectGaps、detectOverlaps、repairGeometryAuto、fixSelfIntersectionByBuffer、fixSelfIntersectionBySplit、measureDistance、measureArea、measureAngle、measureRadius、registerCustomCrs、transformBySevenParameters、transformByFourParameters、calculateSevenParameters、calculateFourParameters、generateLabels、avoidLabelOverlap |
| `src/common/CoordinateUtils.ts` | `default export` (整个模块) | 339 行坐标工具类，无任何业务代码引用。包含 formatCoordinate、parseCoordinateString、parseBatchCoordinates、COORDINATE_BOUNDS、CHINA_BOUNDS、isInChina、isValidLonLat、isValidWebMercator、detectCoordinateType、degreesToRadians、radiansToDegrees、getMetersPerDegree、meterOffsetToDegreeOffset、interpolatePoint、interpolateByDistance |
| `src/common/SpatialAnalysis.ts` | `default export` (整个模块) | 空间分析工具类，无任何业务代码引用 |
| `src/common/GeomValidator.ts` | `default export` | 仅在测试文件 GeomValidator.test.ts 中引用，无业务代码使用 |
| `src/common/UIHelper.ts` | `UIHelper` | 仅在 GisCrs.ts 中引用，需确认 GisCrs 本身是否被使用 |
| `src/components/Logos.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/BottomPanel.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/parser/GeoJSONParser.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/parser/WktParser.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/parser/GisTextParser.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/descriptions/GisDescriptions.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/layouts/GisCard.vue` | 组件 | 无任何业务代码引用（仅 components.d.ts 自动注册） |
| `src/components/data/GisDataInfo.ts` | `EmptyDataInfo` | 仅在 GisDataInfo.ts 自身定义，无任何外部引用 |
| `src/enums/DrawType.ts` | `DrawType` | 仅在 enums/index.ts 重导出，无业务代码引用 |
| `src/event/EventBase.ts` | `EventBase` | 仅在 TipLogger.ts 和 GisMap.ts 中引用 |

### 仅被其他未使用模块引用的链

| 链路 | 说明 |
|---|---|
| `GisAdvancedAnalysis` → `NotImplementedError` | NotImplementedError 仅在 GisAdvancedAnalysis 内部使用，而 GisAdvancedAnalysis 本身未被使用 |
| `DrawType` → `enums/index.ts` → (无消费者) | DrawType 被重导出但无业务代码消费 |
| `UIHelper` → `GisCrs` | UIHelper 仅被 GisCrs 引用，需确认 GisCrs 的使用情况 |

---

## 弃用链分析

> 以下代码存在弃用特征：旧式 API 写法、被新实现替代的遗留代码、或明显过时的模式。

| 文件 | 弃用项 | 替代方案 | 说明 |
|---|---|---|---|
| `src/components/layouts/BaseMain.vue` | 旧式 `defineProps({})` + 手动 slider | `SplitPanel.vue` | BaseMain 使用手动拖拽实现面板分割，SplitPanel 已提供更完善的分割面板实现 |
| `src/components/LeftPanel.vue` | 旧式 `defineProps({})` + 手动绘制工具 | GisData.vue 内的绘制功能 | LeftPanel 是早期开发调试用的绘制工具面板，已被 GisData 主视图中的功能替代 |
| `src/components/BottomPanel.vue` | console-log 事件监听 | `logger` 模块 | BottomPanel 仅用于显示 console-log 事件，实际已被 logger 替代 |
| `src/components/Logos.vue` | Vite/Vue/ElementPlus logo 展示 | 无 | 纯展示组件，无实际业务用途 |
| `src/components/parser/GeoJSONParser.vue` | 独立 GeoJSON 解析面板 | GisDataReader + GeoStrEditor | 已被 GisDataReader 的文件上传 + GeoStrEditor 的编辑器替代 |
| `src/components/parser/WktParser.vue` | 独立 WKT 解析面板 | GisDataReader + GeoStrEditor | 同上 |
| `src/components/parser/GisTextParser.vue` | Tab 切换解析器 | GisDataReader | 已被统一的数据导入流程替代 |
| `src/components/descriptions/GisDescriptions.vue` | 通用描述展示 | 各组件内联 el-descriptions | 仅在 components.d.ts 注册，无实际使用 |
| `src/components/layouts/GisCard.vue` | 自定义卡片容器 | Element Plus el-card | 自定义卡片样式，无业务引用 |
| `src/common/GisAdvancedAnalysis.ts` | 高级分析工具集 | 未实现 | 1363 行代码全部为预留接口，NotImplementedError 占位，无实际功能 |
| `src/common/CoordinateUtils.ts` | 坐标工具集 | `coordinateTransform.ts` + `GeomUtils` | 功能已被 coordinateTransform 和 GeomUtils 覆盖 |
| `src/common/SpatialAnalysis.ts` | 空间分析工具集 | `GeomUtils` + Turf.js 直接调用 | 功能已被 GeomUtils 和组件内 Turf 调用覆盖 |

---

## 代码质量问题

（待补充）

---
