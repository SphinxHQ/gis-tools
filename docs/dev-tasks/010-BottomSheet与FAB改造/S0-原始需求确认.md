# S0: 原始需求确认 — BottomSheet 与 FAB 改造

## 用户原始需求

> Use Skill: dev-task-lite **必须装载 `c:\Users\YuanYu\Desktop\git\gis-tools\.agents\geo-compacter.md` 强化记忆后才能继续后续任务流程**， 按 `c:\Users\YuanYu\Desktop\git\gis-tools\dev-log\temp\ux-renovation-plan.md` 设计的二期改造方案进行推进 ， 并且参考 `c:\Users\YuanYu\Desktop\git\gis-tools\dev-log\temp\phase1-inspection-rules-v2.md`  的检验经验

补充指令：
> 一期可以推送合并到main。然后开始二期

## 任务文档根目录确认
- 默认建议: `docs/dev-tasks/`
- 用户确认路径: `docs/dev-tasks/`（与一期及历史任务一致，未提出异议）
- 本次任务编号: 010
- 本次任务简称: BottomSheet与FAB改造

## 历史任务检查
- 是否找到相似任务: 否（001-009 均为已完成或独立任务，无二期 Bottom Sheet/FAB 相关任务）
- 相似任务路径: 无
- 相似任务状态: 无相关任务
- 处理方式: 新建任务

## 代码初步定位

### 强化记忆装载
- `c:\Users\YuanYu\Desktop\git\gis-tools\.agents\geo-compacter.md`：UI/UX 主理人角色定义与设计规范，已装载为强化记忆上下文
  - 核心约束：单一代码路径原则（禁止 `if (isMobile)` 分支业务逻辑）
  - 触控热区：≥48dp，FAB 56×56dp，手柄 32px
  - 动画：Sheet 300ms Standard easing，FAB 200ms Bouncy easing
  - 拇指热区：FAB 位于右下角绿色区，底部 Tab 栏 56px

### 二期方案依据
- `c:\Users\YuanYu\Desktop\git\gis-tools\dev-log\temp\ux-renovation-plan.md`：二期改造方案
  - 目标：将 `el-drawer` 上滑抽屉改造为符合规范的 Bottom Sheet，新增 FAB 悬浮按钮承载地图控件
  - 核心违规项：2.1 抽屉重挂载（v-if）、2.2 地图控件不可达（左上角红色区）、2.3 无 Bottom Sheet 组件、2.4 Tab 字号 10px 过小

### 检验经验参考
- `c:\Users\YuanYu\Desktop\git\gis-tools\dev-log\temp\phase1-inspection-rules-v2.md`：一期检验规则 V4
  - 5×6 测试矩阵（5 断点 × 6 验收点）
  - L1-L5 五层检验模型（容器/内容/元素/文本/溢出）
  - V3/V4 补充规则（whiteSpace、flex-shrink、canvas 尺寸匹配、弹窗列宽等）
  - DPR 影响与 viewport 设置（CSS 像素 vs 物理像素）

### 相关文件初步定位
| 文件 | 当前状态 | 二期预期改动 |
|------|----------|-------------|
| `src/components/data/GisMobileNav.vue` | 使用 `el-drawer direction="btt"` + `v-if` 切换 Tab，手柄为装饰元素，Tab 字号 10px | 重写：替换为 GisBottomSheet，Tab 内容改 v-show，字号 ≥11px |
| `src/components/gismap/MapControlPanel.vue` | 地图控件固定左上角，按钮无触控热区设定 | 修改：新增 `layout` prop，移动端收起到 FAB |
| `src/components/data/GisDataPanel.vue` | 桌面端 Tab 面板，与 GisMobileNav 逻辑重复 | 修改：提取共享逻辑到 useDataPanelTabs |
| `src/components/data/GisBottomSheet.vue` | 不存在 | 新建：通用底部 Sheet 容器 |
| `src/components/gismap/GisMapFab.vue` | 不存在 | 新建：移动端 FAB 悬浮按钮 |
| `src/composables/useDataPanelTabs.ts` | 不存在 | 新建：共享 Tab 状态管理 |

## AI 复述后的需求

按 `ux-renovation-plan.md` 二期方案，将移动端 `el-drawer` 抽屉改造为符合 `geo-compacter.md` 规范的 Bottom Sheet 组件（拖拽手柄 32px、吸附点 25vh/40vh/80vh、半屏/全屏切换、300ms Standard easing），新增 FAB 悬浮按钮（56×56dp 右下角绿色区、200ms Bouncy easing）承载地图控件（城市选择、CRS 选择、绘制工具），Tab 内容改用 `v-show` 替代 `v-if` 保留状态，提取共享 Tab 逻辑为 `useDataPanelTabs` composable。

参考 `phase1-inspection-rules-v2.md` 的 V4 检验经验，制定二期检验规则（Bottom Sheet 手柄热区、吸附动画、FAB 位置/热区、Tab 状态保留等），并执行 5 断点 × 验收点矩阵测试（MCP Playwright 驱动）。

## 需求边界

### 本次包含
1. 新建 `src/components/data/GisBottomSheet.vue`：通用底部 Sheet 容器（Props: snapPoints/defaultHeight/maxHeight/minHeight；Events: height-change/state-change；拖拽手柄、吸附动画、遮罩层）
2. 新建 `src/components/gismap/GisMapFab.vue`：移动端 FAB 悬浮按钮（Props: items；Events: select；展开 ≤4 个菜单项）
3. 重写 `src/components/data/GisMobileNav.vue`：使用 GisBottomSheet 替代 el-drawer，Tab 内容改 v-show，Tab 字号 ≥11px
4. 新建 `src/composables/useDataPanelTabs.ts`：共享 Tab 状态管理（activeTab/activeData/transformChain/hasData/hasActiveData）
5. 修改 `src/components/gismap/MapControlPanel.vue`：新增 `layout` prop（desktop/fab），移动端收起到 FAB，桌面端按钮热区扩展到 48dp
6. 修改 `src/components/data/GisDataPanel.vue`：复用 useDataPanelTabs，消除与 GisMobileNav 的逻辑重复

### 本次不包含
- 手势增强（长按选点、左滑操作、双击放大）—— 第三期
- Web Worker 解析大文件 —— 第三期
- 虚拟滚动优化要素列表 —— 第三期
- 设计 Token 化与视觉一致性 —— 第四期
- 代码清理、可访问性、目录重构 —— 第五期

## 用户确认记录
- 结论: 已确认（通过 AskUserQuestion 封闭式问答）
- 用户补充:
  - 推送处理：先排查网络再继续（网络已恢复，一期已推送成功）
  - 分支策略：新建 feature/phase2 (推荐)
  - 流程开关：启用验收确认 S7

## 版本控制
- 版本控制系统: git
- 当前状态:
  - 当前分支: `feature/phase2-bottom-sheet-fab`（基于 main b5e4496 新建）
  - 未提交改动: 无
  - 与远端差异: main 已与 origin/main 一致（b5e4496，一期已推送成功）
  - 最近提交摘要:
    - b5e4496 merge: 一期布局统一与架构重构完成
    - 63c7613 docs: 补全历史任务文档（001-008）
    - 281c490 feat: 一期布局统一与架构重构完成
- AI 建议: 在 feature/phase2-bottom-sheet-fab 分支上推进二期，完成后合并回 main
- 用户选择（封闭式问答）:
  - [x] 在 AI 建议的新分支上进行（feature/phase2-bottom-sheet-fab）
  - [ ] 在用户指定的现有分支上进行
  - [ ] 接受在主线直接修改
  - [ ] 不使用版本控制
- 最终采用: `feature/phase2-bottom-sheet-fab` 分支

## 流程配置
- 技术栈: Vue 3 + TypeScript + Vite + Element Plus + OpenLayers 10
- 启用验收确认 S7: 是（用户显式选择）
- 启用编译验证: 是（默认，npm run build）
- 启用推送: 是（默认，S8 执行 git push）
- 改动评估: 是（默认，S3 使用 code-change-evaluator-mini 评分）
