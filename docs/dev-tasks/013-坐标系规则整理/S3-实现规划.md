# S3: 实现规划 — 坐标系规则整理

## 实现步骤清单（按执行顺序）

### 阶段 A: 文档建立（先建立规则源）

| 步骤 | 文件 | 操作 | 说明 |
|------|------|------|------|
| A1 | `docs/dev-tasks/013-坐标系规则整理/坐标系规则规范.md` | 新建 | 5 条规则成文，含术语、关系图、违反处理流程 |
| A2 | `docs/dev-tasks/003-数据生命周期/S1-需求分析.md` | 追加 | 末尾追加"修订记录"段，引用 013 规范 |
| A3 | `docs/dev-tasks/911-遗留问题.md` | 追加 | 末尾追加"三、坐标系规则违反（任务 013）"段，3 条 bug 登记 |

### 阶段 B: 代码修复（按规则编号）

| 步骤 | 文件 | 操作 | 规则 | 说明 |
|------|------|------|------|------|
| B1 | `src/components/data/GisDataTransformer.vue` | 修改 handleResetCrs | R3、R4 | 改为 addDataset 创建同源新数据集，原数据集保持不变；新数据集作为独立节点不继承路径 |
| B2 | `src/components/data/GisDataTransformer.vue` | 修改 reloadVersionsData | R2、R3 | 返回值改为 allSuccess 语义；失败时不影响 data（已是 origin clone） |
| B3 | `src/components/data/GisDataTransformer.vue` | 修改 watch props.data | R2、R3 | crsVersions[0].data 改用 GisDataInfo.clone；reloadVersionsData 失败时 activeVersionName 回退 origin |
| B4 | `src/components/data/GisData.vue` | 新增 watch datasets | R2 | 监听 id 数组变化，数据集删除时清理 datasetActiveDataMap 缓存 |

### 阶段 C: 代码注释（引用规则编号）

| 步骤 | 文件 | 位置 | 规则 |
|------|------|------|------|
| C1 | `src/components/gismap/GisMapSlot.vue` | epsgCode computed 上方 | R2 |
| C2 | `src/components/data/GisData.vue` | `<gis-map-slot v-for>` 上方 | R1 |
| C3 | `src/components/data/GisData.vue` | datasetActiveDataMap 上方（含改动 B4 注释） | R2 |
| C4 | `src/components/data/GisDataTransformer.vue` | handleResetCrs 上方（含改动 B1 注释） | R3、R4 |
| C5 | `src/components/data/GisDataTransformer.vue` | addTransformVersion 上方 | R4 |
| C6 | `src/components/data/GisDataTransformer.vue` | reloadVersionsData 上方（含改动 B2 注释） | R2、R3 |
| C7 | `src/components/data/GisFeatureTree.vue` | promptUpdateOrSaveAs 上方 | R5 |

### 阶段 D: 验证

| 步骤 | 命令/操作 | 说明 |
|------|----------|------|
| D1 | `npm run type-check` | TypeScript 类型检查 |
| D2 | `npm run build` | Vite 构建 |
| D3 | 人工核对规则文档 | 5 条规则成文完整、911 登记 3 条、003 修订记录已加 |

## 并行执行策略

- A1、A2、A3 可并行（文档独立）
- B1、B2、B3 同文件，按顺序串行
- B4 独立文件，可与 B1~B3 并行
- C1~C7 注释与其对应的代码改动合并执行（不单独成步）
- D1、D2 可并行（type-check 和 build 独立）

## 验收标准

1. 5 条规则在《坐标系规则规范.md》中成文，每条含陈述、动机、实现位置、违反后果、验证方法
2. 003 S1 末尾有修订记录引用 013
3. 911 末尾有 3 条 bug 登记（状态 ✅ 已解决）
4. handleResetCrs 调用 addDataset，不再调用 updateDataset
5. reloadVersionsData 失败时 activeVersionName 回退 origin
6. crsVersions[0].data 使用 GisDataInfo.clone
7. datasetActiveDataMap 在数据集删除时清理
8. 7 处注释引用 R1~R5 规则编号
9. type-check 通过
10. build 通过
