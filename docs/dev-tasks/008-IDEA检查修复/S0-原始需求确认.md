# S0: 原始需求确认 — IDEA检查修复

## 用户原始需求

根据 IntelliJ IDEA 代码检查报告（SARIF 格式），修复项目中的代码质量问题。报告共 429 个问题，按类型分布：

| 数量 | 类型 | 说明 |
|---|---|---|
| 132 | Eslint | ESLint 规则违规 |
| 104 | JSUnusedGlobalSymbols | 未使用的全局符号 |
| 66 | CssUnusedSymbol | 未使用的 CSS 选择器 |
| 21 | ES6UnusedImports | 未使用的 import |
| 18 | JSIgnoredPromiseFromCall | Promise 未 await/处理 |
| 12 | DuplicatedCode | 重复代码 |
| 10 | TypeScriptUMDGlobal | UMD 全局变量引用 |
| 7 | JSDeprecatedSymbols | 使用了已弃用符号 |
| 7 | GrazieInspection | 语法/拼写检查 |
| 6 | VueMissingComponentImport | Vue 组件缺少 import |
| 6 | CssOverwrittenProperties | CSS 属性覆盖 |
| 6 | JSUnusedLocalSymbols | 未使用的局部变量 |
| 5 | UnnecessaryLocalVariableJS | 冗余局部变量 |
| 其余 45 | 混合 | Annotator/JSCommentMatchesSignature/ExceptionCaughtLocallyJS 等 |

报告文件：`dev-log/report_2026-06-25_12-38-13.sarif.json`

## 任务文档根目录确认
- 默认建议: `docs/dev-tasks/`
- 用户确认路径: `docs/dev-tasks/`

## 历史任务检查
- 是否找到相似任务: 否
- 处理方式: 新建任务（008）

## 代码初步定位
- 报告覆盖 src 目录下的 .ts/.vue 文件
- 主要问题集中在：未使用代码（191项）、ESLint 违规（132项）、CSS 问题（72项）
- 007 任务已注释掉大量未使用模块，但报告可能是注释前生成的，部分问题可能已解决

## AI 复述后的需求

根据 IntelliJ IDEA 检查报告，按 AI 能力范围修复可自动处理的代码质量问题：
1. **ES6UnusedImports**（21项）：删除未使用的 import
2. **JSUnusedLocalSymbols**（6项）：删除未使用的局部变量
3. **UnnecessaryLocalVariableJS**（5项）：内联冗余局部变量
4. **JSIgnoredPromiseFromCall**（18项）：添加 void 或 await
5. **CssUnusedSymbol**（66项）：删除未使用的 CSS 选择器（仅 scoped 局部）
6. **CssOverwrittenProperties**（6项）：修复 CSS 属性覆盖
7. **JSUnusedGlobalSymbols**（104项）：逐个确认引用链后注释/删除未使用全局符号
8. **Eslint**（132项）：修复 ESLint 规则违规
9. **TypeScriptUMDGlobal**（10项）：修复 UMD 全局引用，改为显式 import
10. **JSDeprecatedSymbols**（7项）：替换已弃用符号为推荐 API

不处理的类型：
- DuplicatedCode（12项）：用户明确选择不处理
- GrazieInspection（7项）：拼写/语法建议，IDEA 误报
- VueMissingComponentImport（6项）：自动注册组件，IDEA 误报

## 需求边界
- 本次包含: 按 AI 能力修复 SARIF 报告中的代码质量问题（含扩展范围）
- 本次不包含:
  - 不处理 DuplicatedCode（用户明确排除）
  - 不修改业务逻辑
  - 不处理 IDEA 误报（GrazieInspection、VueMissingComponentImport）
  - CSS 选择器不处理全局注入内容（:deep()、Element Plus 覆盖等）

## 用户确认记录
- 结论: 已确认
- 用户补充:
  1. 版本控制：直接在 main 修改
  2. 修复范围：ES6UnusedImports、JSUnusedLocal/UnnecessaryVar、JSIgnoredPromiseFromCall、CssUnused/Overwritten
  3. CSS 选择器：只处理 scoped 局部的，不处理全局显式注入的内容（如 :deep()、Element Plus 覆盖等）

## 用户二次确认记录（解除微创限制）
- 结论: 已确认扩展修复范围
- 用户补充:
  1. 解除【微创】限制，重新审查任务
  2. 扩展修复范围至：
     - JSUnusedGlobalSymbols（104项）— 未使用全局符号，逐个确认引用链后注释/删除
     - Eslint（132项）— ESLint 规则违规
     - TypeScriptUMDGlobal（10项）+ JSDeprecatedSymbols（7项）— UMD 全局引用 + 已弃用符号
  3. DuplicatedCode（12项）不处理（用户明确选择"不处理 DuplicatedCode"）
  4. VueMissingComponentImport（6项）+ GrazieInspection（7项）保持排除（IDEA 误报）

## 版本控制
- 版本控制系统: git
- 当前状态:
  - 当前分支: main
  - 未提交改动: 无
  - 与远端差异: 一致
  - 最近提交摘要: refactor: comment out unused vue components and deprecated files
- AI 建议: 直接在 main 修改（纯代码质量修复，无逻辑变更）
- 用户选择: 接受在主线直接修改（原因：纯代码质量修复，无逻辑变更）
- 最终采用: main 分支直接修改

## 流程配置
- 技术栈: Vue 3 + TypeScript + Vite + Element Plus
- 启用验收确认 S7: 否
- 启用编译验证: 是（npm run build + npm run typecheck）
- 启用推送: 是
- 改动评估: 否（代码质量修复，改动范围明确且低风险）
