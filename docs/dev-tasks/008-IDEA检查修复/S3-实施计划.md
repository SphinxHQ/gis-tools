# S3: 实施计划 — IDEA检查修复

## 修改项清单

### M1: ES6UnusedImports — 删除未使用 import（21项）✅ 已完成
- 文件: useMapController.ts, GisFeatureTree.vue, GisFeatureEditor.vue, GeomUtils.test.ts, GisCrsSelector.vue, GisDataTransformer.vue, MapControlPanel.vue, GisDataValidator.vue, GisStyle.ts, GisDataReader.vue
- 修改内容: 删除未使用的 import 语句
- 影响面: 无（纯删除未使用代码）
- 验证方法: npm run build
- 回滚方式: git revert

### M2: JSUnusedLocal/UnnecessaryVar — 删除未使用局部变量（11项）✅ 已完成
- 文件: GeomValidator.ts, GisDataTransformer.vue, GisData.vue, ExfPaser.js, GisLayer.ts, GisCrs.ts, GisFeatureEditor.vue, DefaultStyle.ts, useBreakpoint.ts, SplitPanel.vue, entryLoader.ts
- 修改内容: 删除未使用变量或内联冗余变量
- 影响面: 无
- 验证方法: npm run build
- 回滚方式: git revert

### M3: JSIgnoredPromiseFromCall — 添加 void（18项）✅ 已完成
- 文件: eventBus.ts, useMapController.ts, GisMap.ts, main.ts
- 修改内容: 对不需要等待的 Promise 调用添加 `void` 前缀
- 影响面: 无（void 只是显式忽略返回值）
- 验证方法: npm run build
- 回滚方式: git revert

### M4: CssOverwrittenProperties + CssUnusedSymbol(scoped 普通) — CSS 清理（18项）
- **CssOverwrittenProperties（6项）**:
  - GisMapBase.vue:373,374,383,384 — border 与 border-width 冲突，合并声明
- **CssUnusedSymbol scoped 普通（12项）**:
  - SplitPanel.vue:307,323,326,331,338 — 删除空规则 .vertical / .split-panel-divider
  - GisData.vue:460,497 — 删除 .top-bar-logo / .dataset-badge
  - GisDataReader.vue:311 — 删除 .gis-data-reader-footer
  - GisDataValidator.vue:488 — 删除 .label2
  - GisFeatureTree.vue:646 — 删除 .label2
  - CrsInfoRender.vue:153 — 删除 .crs-info-render-label
- 不处理: 全局样式（31项）、scoped :deep()（23项）
- 影响面: 仅样式，不影响功能
- 验证方法: npm run build + 视觉检查
- 回滚方式: git revert

### M5: TypeScriptUMDGlobal + JSDeprecatedSymbols — UMD 与弃用符号（17项）
- **TypeScriptUMDGlobal（10项）**:
  - ExfDataFormat.ts:27,28 — 添加 `import type * as GeoJSON from 'geojson'`
  - WktDataFormat.ts:27,31,83,88 — 同上
  - useMapController.ts:86,108,109,110 — 同上（注意 M1 已删除 GeoFeature import，需确认 GeoJSON 命名空间用法）
- **JSDeprecatedSymbols（7项）**:
  - Common.test.ts:41-47 — isPrimitive 已弃用，替换为推荐 API（需查看具体上下文确定替代方案）
- 影响面: 低（类型 import 不影响运行时）
- 验证方法: npm run build + npm run typecheck
- 回滚方式: git revert

### M6: Eslint — ESLint 规则违规修复（132项）
- **自动修复（约 79 项）**:
  - import/order（34项）— `npx eslint --fix`
  - vue/attributes-order（28项）— `npx eslint --fix`
  - vue/html-closing-bracket-newline（17项）— `npx eslint --fix`
- **与 M1/M2 重叠（约 42 项）**:
  - @typescript-eslint/no-unused-vars（27项）— 报告为旧，M1/M2 已处理大部分，剩余重新检查
  - unused-imports/no-unused-imports（15项）— 同上
- **手动处理（11 项）**:
  - no-console（5项）— GisFeatureEditor.vue，改为 logger 或保留必要 console
  - vue/no-mutating-props（2项）— GisFeatureTree.vue，改为 emit 或本地副本
  - no-undef（1项）— GisMapBase.vue:139 SysGisMapLayer 引用
  - vue/html-self-closing（1项）、vue/html-closing-bracket-spacing（1项）、vue/no-unused-vars（1项）
- 影响面: 中（自动修复可能调整大量 import 顺序）
- 验证方法: npm run build + npm run lint
- 回滚方式: git revert
- 执行策略: 先运行 eslint --fix，再重新检查剩余问题

### M7: JSUnusedGlobalSymbols — 未使用全局符号清理（104项）
- **优先评估（51 项）**:
  - src/test/shared-test-data.ts（36项）— 确认测试文件是否引用，无引用则块注释
  - src/types/gis.ts（15项）— 确认类型引用链，无引用则块注释
- **逐个确认（53 项）**:
  - GeomValidator.ts（7项）、GisMap.ts（5项）、GisStyle.ts（4项）等 31 个文件
  - 每个符号用 Grep 搜索引用链，无引用则块注释（遵循 007 任务规范）
- 修复方式: 块注释 `/* */`（遵循 007 任务规范，不用 @deprecated）
- 影响面: 中（需逐个核实引用链，避免误删）
- 验证方法: npm run build + npm run typecheck
- 回滚方式: git revert

## 执行顺序
1. M4（CSS 清理）→ 2. M5（UMD + 弃用符号）→ 3. M6（ESLint 修复）→ 4. M7（全局符号清理）→ 5. 构建验证并推送

## 风险控制
- 每个 M 完成后运行 `npm run build` 验证
- M6 自动修复后需人工检查 diff，确认无逻辑变更
- M7 每个符号删除前必须 Grep 确认无引用
- 全部完成后运行 `npm run build && npm run typecheck` 最终验证
